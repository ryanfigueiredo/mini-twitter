<section class="section">
  <div class="container">
    <div class="columns">
    <% if user_signed_in? %>
      <%= render 'profile' %>
    <% else %>
      <%#= render 'trends' %>
    <% end %>
      <%= render 'feed', tweets: @tweets, tweet: @tweet %>

      <%= render 'who_to_follow', users: @users %>
    </div>
  </div>
</section>
